# ~/.codex/config.toml — add the [mcp_servers.drift] block.
# Restart Codex after editing; the six drift_* tools will appear.

[mcp_servers.drift]
command = "npx"
args = ["-y", "@drift/mcp"]
env = { DRIFT_REPO = "/abs/path/to/your/repo" }
