{
  "name": "drift",
  "version": "0.2.1",
  "description": "Intent-Driven Versioning — semantic commits, blame by intent, MCP tools for Gemini CLI.",
  "mcpServers": {
    "drift": {
      "command": "node",
      "args": ["${extensionPath}/packages/drift-mcp/dist/index.js"],
      "env": {
        "DRIFT_REPO": "${DRIFT_REPO}"
      }
    }
  },
  "settings": [
    {
      "name": "Drift repository",
      "description": "Absolute path to the repository Drift should operate on.",
      "envVar": "DRIFT_REPO"
    }
  ]
}
